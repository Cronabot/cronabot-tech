import { FC } from 'react';
import Navbar from '@components/Navbar';

interface Props {}

const About: FC<Props> = ({}) => {
    return (
        <>
            <Navbar />
            About
        </>
    );
};

export default About;
